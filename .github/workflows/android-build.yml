name: Android Build & Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*.*.*'  # 当创建类似 v1.0.0 的tag时触发发布
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v2
      
    - name: Create local.properties
      run: |
        echo "sdk.dir=/usr/local/lib/android/sdk" > local.properties
        echo "创建 local.properties 文件"
      
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build Debug APK
      run: |
        ./gradlew assembleDebug --no-daemon --stacktrace
      
    - name: Build Release APK
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        ./gradlew assembleRelease --no-daemon --stacktrace
        
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: pdf-reader-debug-apk
        path: app/build/outputs/apk/debug/*.apk
        
    - name: Upload Release APK
      if: startsWith(github.ref, 'refs/tags/v')
      uses: actions/upload-artifact@v4
      with:
        name: pdf-reader-release-apk
        path: app/build/outputs/apk/release/*.apk
        
    # ========== 自动发布到 GitHub Releases ==========
    - name: Create GitHub Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        name: PDF Reader ${{ github.ref_name }}
        body: |
          ## 📱 PDF Reader ${{ github.ref_name }}
          
          ### 🚀 新版本发布
          
          一个简洁的PDF阅读器应用，支持：
          - 📄 PDF文件浏览
          - 🌙 夜间模式
          - 📖 整页/半页阅读模式
          - 📍 阅读进度保存
          
          ### 📦 包含文件
          - Release APK（正式版，可直接安装）
          
          ### 🔧 安装说明
          1. 下载APK文件
          2. 在手机上找到下载的文件
          3. 点击安装
          4. 如需，开启"未知来源应用"安装权限
          
          ### 📝 更新日志
          - 初始版本发布
          
          ### 🐛 问题反馈
          如有问题，请在 [Issues](https://github.com/chixiao994/pdf-reader/issues) 页面反馈。
          
        files: |
          app/build/outputs/apk/release/*.apk
        draft: false  # 立即发布，不是草稿
        prerelease: false  # 不是预发布版本
        generate_release_notes: true  # 自动生成发布说明
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
